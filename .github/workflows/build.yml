name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-build.txt
        pip install pyinstaller
    
    - name: Build with PyInstaller
      run: |
        pyinstaller --onefile --windowed --name antic --add-data "GeoLite2-City.mmdb;." --add-data "GeoLite2-Country.mmdb;." --add-data "extensions;extensions" antic.py
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: dist/antic.exe
        body: |
          ## 🚀 Antic Browser ${{ github.ref_name }}
          
          Автоматическая сборка версии ${{ github.ref_name }}
          
          ### Скачать:
          - [antic.exe](https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/antic.exe)
          
          ### Что нового:
          - Автоматическое обновление
          - Интеграция с CyberYozh и SX.ORG
          - Расширение CyberYozh закреплено
          - GEO автонастройка под прокси
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
